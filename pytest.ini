[pytest]
asyncio_mode = auto
testpaths = tests features
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    asyncio: mark test as async
addopts =
    -v
    --strict-markers
    --tb=short